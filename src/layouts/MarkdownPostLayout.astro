---
const { frontmatter } = Astro.props;
---
<h1>{frontmatter.title}</h1>
<p>{frontmatter.description} - by {frontmatter.author} on {frontmatter.pubDate.toString().slice(0,10)}</p>
<p>{frontmatter.tags.map((tag) => <span>[{tag}] </span>)}</p>
<img src={frontmatter.image.url} alt={frontmatter.image.title} style="width: 100%; height: auto;"/>
<slot />